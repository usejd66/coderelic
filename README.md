# CodeRelic

CodeRelic is a CLI tool that helps developers identify code that may have been abandoned in a Git repository.

Instead of assuming that old code is dead, CodeRelic combines multiple signals such as Git history, imports, test references, feature areas, and framework/runtime patterns.

## Installation

```bash
npm install
npm link